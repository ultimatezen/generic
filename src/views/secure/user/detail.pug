extends ../../layout.pug

block content
	h1 User Detail

	li userId #{id}
	li email #{email}

	form(action="/cms/secure/user/" + id + "/delete", method="POST")
		.form-group
			input(type="submit", class="btn btn-primary", value="delete")
