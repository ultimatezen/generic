extends ../../layout.pug

block content
	h1 Plan Detail

	li name #{name}
	li desc #{desc}
	li area #{area}
	li genre #{genre}
	li requiredTime #{requiredTime}
	li status #{status}
	li lang #{lang}

	form(action="/cms/secure/plan/" + id + "/delete", method="POST")
		.form-group
			input(type="submit", class="btn btn-primary", value="delete")
